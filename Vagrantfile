# -*- mode: ruby -*-
# vi: set ft=ruby :

# Bring in some code to verify plugins and create asm disks
require_relative 'lib/plugin_manager.rb'
require_relative 'lib/disk_manager.rb'

# Vagrantfile API/syntax version. Don't touch unless you know what you're doing!
VAGRANTFILE_API_VERSION = "2"

# Select which 'sub-Vagrantfile' to load

#require_relative 'lib/oradb12-single.rb'
require_relative 'lib/oradb12-rac-2-node.rb'
